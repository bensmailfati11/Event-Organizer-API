# TODO: Remove Handlebars from Event Organizer API

- [x] Remove 'express-handlebars' and 'handlebars' from package.json dependencies
- [x] Remove Handlebars engine setup from src/main.js
- [x] Change the root route in src/routes/index.js to return JSON instead of rendering a view
- [x] Delete the views/ directory
- [x] Run npm install to update dependencies
- [ ] Add and commit the changes with message "Remove Handlebars templating"
